# 照猫画虎

!!! quote 好记性不如烂键盘，动手才是硬道理

## 先来九个

!!! example 示例1：打印字符串

```python
print("Hello, world!")
# Hello, world!
```

!!! example 示例2：输出变量值

```python
i = 256*256

print('i 的值为：', i)
# i 的值为： 65536
```

!!! example 示例3：定义变量并进行简单的数学运算

```python
x = 3
y = 2
z = x + y

print(z)
# 5
```

!!! example 示例4：定义一个列表并打印出其中的元素

```python
my_list = ['Peking’, 'University', 'I love Python']

print(my_list[0])   # 输出 "Peking"
print(my_list[1])   # 输出 "University"
print(my_list[2])   # 输出 "I love Python"
```

!!! example 示例5：使用 for 循环打印数字 0 到 4

```python
for i in range(5):
    print(i)
```

!!! example 示例6：根据条件输出不同的结果

```python
x = 6
if x > 10:
    print("x 大于 10")
else:
    print("x 小于或等于 10")
```

!!! example 示例7：尝试来写一个斐波纳契数列

    斐波那契数列是一个经典的数学问题，其中每个数字是前两个数字之和。

```python
# Fibonacci series: 斐波纳契数列
# 两个元素的总和确定了下一个数
a, b = 0, 1
while b < 10:
    print(b)
    a, b = b, a+b
```

其中代码 a, b = b, a+b 的计算方式为先计算右边表达式，然后同时赋值给左边，等价于：

```python
t1 = b
t2 = a+b
a = t1
b = t2
```

!!! example 示例8：使用 for 循环来写一个斐波纳契数列

```python
n = 10
a, b = 0, 1
for i in range(n):
    print(b)
    a, b = b, a + b
```

!!! example 示例9：`end` 参数调整输出

    `end` 参数可以用于将结果输出到同一行，或者在输出的末尾添加不同的字符

```python
# Fibonacci series: 斐波纳契数列
# 两个元素的总和确定了下一个数
a, b = 0, 1
while b < 1000:
    print(b, end=',')
    a, b = b, a+b
```
